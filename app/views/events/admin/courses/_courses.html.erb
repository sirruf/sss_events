<table class="table table-striped">
  <thead>
  <tr>
    <th width="85%">Название</th>
    <th width="5%">Событий</th>
    <th width="10%"></th>
  </tr>
  </thead>
  <tbody>
  <% @courses.each do |course| %>
      <tr>
        <td>
          <%= course.name %>
        </td>
        <td class="text-center">
          <%= course.events.count %>
        </td>
        <td class="text-center">
          <%= link_to edit_admin_course_path(course), class: 'btn btn-xs btn-default' do %>
              <%= fa_icon 'edit' %>
          <% end %>
          <% if course.events.count == 0 %>
              <%= link_to admin_course_path(course), class: 'btn btn-xs btn-danger', method: :delete do %>
                  <%= fa_icon 'trash' %>
              <% end %>
          <% end %>

        </td>
      </tr>
  <% end %>
  </tbody>
</table>